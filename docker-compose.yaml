services:
  auth-db:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: auth
    volumes:
      - auth_db_data:/var/lib/postgresql
    ports:
      - "5433:5432"

  billing-db:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: billing
    volumes:
      - billing_db_data:/var/lib/postgresql
    ports:
      - "5434:5432"

  payment-db:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: payment
    volumes:
      - payment_db_data:/var/lib/postgresql
    ports:
      - "5435:5432"

  kafka:
    image: apache/kafka:latest
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: "broker,controller"
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@kafka:9093"

      KAFKA_LISTENERS: "INTERNAL://0.0.0.0:29092,EXTERNAL://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093"

      KAFKA_ADVERTISED_LISTENERS: "INTERNAL://kafka:29092,EXTERNAL://localhost:9092"

      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT,CONTROLLER:PLAINTEXT"
      KAFKA_CONTROLLER_LISTENER_NAMES: "CONTROLLER"
      KAFKA_INTER_BROKER_LISTENER_NAME: "INTERNAL"

      KAFKA_LOG_DIRS: "/var/lib/kafka/data"
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    volumes:
      - kafka_data:/var/lib/kafka/data
      - kafka_secrets:/etc/kafka/secrets
      - kafka_config:/mnt/shared/config

  auth-service:
    build:
      context: .
      dockerfile: ./services/auth/Dockerfile
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
      - DB_CONN=postgres://postgres:1234@auth-db:5432/auth?sslmode=disable
      - KAFKA_BROKER=kafka:29092
      - JWT_SECRET=my_super_secret_key
    depends_on:
      - auth-db
      - kafka

  payment-service:
    build:
      context: .
      dockerfile: ./services/payment/Dockerfile
    ports:
      - "8082:8082"
    environment:
      - PORT=8082
      - DB_CONN=postgres://postgres:1234@payment-db:5432/payment?sslmode=disable
      - KAFKA_BROKER=kafka:29092
    depends_on:
      - payment-db
      - kafka

  billing-service:
    build:
      context: .
      dockerfile: ./services/billing/Dockerfile
    ports:
      - "8083:8083"
    environment:
      - PORT=8083
      - DB_CONN=postgres://postgres:1234@billing-db:5432/billing?sslmode=disable
      - KAFKA_BROKER=kafka:29092
    depends_on:
      - billing-db
      - kafka

  gateway-service:
    build:
      context: .
      dockerfile: ./services/gateway/Dockerfile
    ports:
      - "8084:8084"
    environment:
      - PORT=8084
      - AUTH_ADDRESS=auth:8081
      - PAYMENT_ADDRESS=payment:8082
      - BILLING_ADDRESS=billing:8083
      - JWT_SECRET=my_super_secret_key

  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    ports:
      - "8080:8080"
    depends_on:
      - kafka
    environment:
      KAFKA_CLUSTERS_0_NAME: "local-cluster"
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: "kafka:29092"
      DYNAMIC_CONFIG_ENABLED: "true"

  loki:
    image: grafana/loki:latest
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - loki_data:/loki

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - loki

  alloy:
    image: grafana/alloy:latest
    volumes:
      - ./config.alloy:/etc/alloy/config.alloy:ro
      - /var/run/docker.sock:/var/run/docker.sock
    command:
      - run
      - --server.http.listen-addr=0.0.0.0:12345
      - /etc/alloy/config.alloy
    ports:
      - "12345:12345"
    depends_on:
      - loki

volumes:
  auth_db_data:
  billing_db_data:
  payment_db_data:

  kafka_data:
  kafka_secrets:
  kafka_config:

  grafana_data:
  loki_data: