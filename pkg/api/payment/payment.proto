syntax = "proto3";

package payment;

option go_package = "pkg/api/payment;payment";

service PaymentService {
  rpc Pay (PaymentRequest) returns (PaymentResponse);
}

message PaymentRequest {
  int32 user_id = 1;
  double amount = 2;
}

message PaymentResponse {
  string message = 1;
}